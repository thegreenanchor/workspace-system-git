# Branded Social Visual

This file replaces the original packaged `.skill` artifact that existed in the private workspace.

The original export contained a broken binary bundle that was not safe or useful to keep in the public repository. It has been replaced with this plain-text placeholder so the repo stays readable and portable.

## Intended purpose

The original asset was used as a helper for generating branded social visuals inside the MNA marketing team workflow.

## Public-repo note

If this repository ever needs a shareable replacement, add one of these instead:

- a plain Markdown skill spec
- a documented prompt template
- a small JSON or YAML config describing the visual workflow
- a clean packaged asset that can be opened and validated outside the private workspace
